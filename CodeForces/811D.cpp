#include <bits/stdc++.h>

using namespace std;

char a[101][101];

int main()
{
	int n,m,x,y,px,py;
	scanf("%d %d\n",&n,&m);
	for(int i = 1;i <= n;i ++)
		gets(a[i]);
	printf("R\n");
	while(1)
	{
		scanf("%d %d",&x,&y);
		if(a[x][y] != )
	}
}
